x <- (Int);
x <- readin();

func teste(a (Int)) -> {
    if (a > 2) {
        print("maior");
    }
    else{
        print("menor");
    };
    return 0;
} (Int);

y <- (Int);
y <- teste(x);

print(y);
